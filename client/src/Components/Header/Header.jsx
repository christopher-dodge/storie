import React from 'react';
import NavDrawer from '../../Components/NavDrawer';

export const Header = () => {
  return (
    <header>
      <div>
        <NavDrawer />
      </div>
    </header>
  );
};

export default Header;